<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>
    <style name="CustomList" parent="Theme.AppCompat.Light">
        <item name="android:background">@drawable/custom_style</item>
    </style>

</resources>
